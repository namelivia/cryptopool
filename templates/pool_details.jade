template(name='poolDetails')
	div(class='container-fluid')
		div(class='row')
			h3(class='text-center') Pool details
		div(class='row')
			div(class='col-md-3 col-md-offset-1')
				h3 Amount: #{amount}
				h3 Status id: #{status_id}
				h3 User count: #{userCount}
				h3 Total amount: #{totalAmount}
				if userAlreadyIn
					h3 Your current score:
					h3 X days left for the match
					h3 Bitcoin address: #{bitcoinAddress}
				else
					button(class='btn btn-primary btn-success join' value='Participate') Participate
		div(class='row')
			+userList(users=participants)
